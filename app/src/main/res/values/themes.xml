<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark navy window background so there is no white flash before Compose draws. -->
    <style name="Theme.MoraWallet" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/mora_window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
